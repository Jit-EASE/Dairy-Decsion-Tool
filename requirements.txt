# === Core (runs the app end-to-end with TF-IDF RAG + no RL) ===
streamlit
numpy
pandas
plotly
scikit-learn
openai

# Maps (optional but recommended for the geospatial header)
folium
streamlit-folium

# PDF ingestion (optional; plain TXT fallback exists)
# PyPDF2

# VRP / Routing (optional; if omitted, map shows but routes/VRP cost are disabled)
# ortools can be heavy; install only if you need the VRP solver
ortools

MILP # (optional; enables the Commercial Plan tab’s optimizer)
pulp

Dense-RAG # (optional; app falls back to TF-IDF if absent)
sentence-transformers
faiss-cpu

#Reinforcement Learning (PPO) — OPTIONAL and TensorBoard-FREE
# If enabling RL, DO NOT install tensorflow/tensorboard.
gymnasium
stable-baselines3
torch
